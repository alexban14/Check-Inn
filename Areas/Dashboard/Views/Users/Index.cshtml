@model Check_Inn.Areas.Dashboard.ViewModels.UserListingModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Dashboard/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col col-lg-12">
        <h2>Users</h2>
        <hr />
    </div>
</div>

<div class="row mb-3">
    <div class="col col-lg-12">
        <form>
            <div class="input-group">
                <input type="text" name="searchterm" class="form-control" placeholder="Search" value="@Model.SearchTerm" />
                @if (Model.Roles != null && Model.Roles.Count() > 0)
                {
                    <select class="form-select" name="AccomodationTypeID" id="AccomodationTypeID">
                        <option>Filter by User Role</option>
                        @foreach (var role in Model.Roles)
                        {
                            <option value="@role.Id" @(role.Id == Model.RoleID ? "selected" : string.Empty)>
                                @role.Name
                            </option>
                        }
                    </select>
                }
                <button class="btn btn-outline-primary" type="submit">
                    <i class="fa-solid fa-magnifying-glass me-1"></i> Search
                </button>
                <a href="@Url.Action("Index", "Users")" class="btn btn-outline-secondary">
                    <i class="fa-solid fa-rotate-right me-1"></i> Reset
                </a>
                <a href="@Url.Action("Action", "Users")" id="createButton" class="btn btn-outline-success" type="button">
                    <i class="fa-solid fa-plus me-1"></i> Create
                </a>
            </div>
        </form>
    </div>
</div>

<div class="row">
    <div class="col col-lg-12">
        @{ Html.RenderPartial("_Listing", Model); }
    </div>
</div>


